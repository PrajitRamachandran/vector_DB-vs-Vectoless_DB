# ── Streamlit UI ────────────────────────────────────────
streamlit>=1.35.0

# ── Visualisation (already in main requirements, listed for clarity) ─────────
matplotlib>=3.8.0
pandas>=2.0.0

# ── All other dependencies come from the project's requirements.txt ──────────
# Install both:
#   pip install -r requirements.txt
#   pip install -r requirements_streamlit.txt
